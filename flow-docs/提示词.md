
你的理解存在偏差
- 当拖入画布的是排他网关时，创建默认分支和一个排他分支就可以
- 当拖入画布的是包容网关时，创建默认分支和两个普通分支，并且包容网关的分流和聚合成对出现，并且有关联关系

请继续修改


普通分支1和普通分支2的间距是合适的，但是普通分支2和默认分支的间距太小，请继续调整修改

现在要求网关分流的起始连线都从网关的一个点开始

网关聚合的接入连线都从网关结束的一个点结束

------------------------------------------------------------------
1、现在要求默认分支后的连线和连线后的第一个节点都不允许删除，请修改删除验证逻辑

2、要求两个节点之间的连线不能重复，只允许出现一条线

3、连线的起点和终点不能是同一个节点

4、包容网关分流起始节点点击时，请开启到包容网关聚合结束节点连线的动画，点击其他节点时需要关闭进行中的动画

5、包容网关创建时起始节点的名称需要保持一致，包容网关分流起始节点和聚合结束节点的名称单独修改时需要同时起始更新节点名称

6、一个包容网关作为另外一个包容网关的一个分支时，两个包容网关之间现在无法连线

7、包容网关之间的所有节点都需要添加完整的 forkGatewayId 和 joinGatewayId 属性，请添加

8、网关删除逻辑要求
**排他网关**
- [删除联动]排他网关节点删除时需要删除默认分支连线和默认分支节点，现在是默认节点没有删除
- [删除限制]排他网关的默认分支连线和默认连线后的第一个节点不允许单独删除，仅支持和排他网关节点一起删除

**包容网关**
- [删除联动]删除时，包容网关的默认分支连线需要删除，连线后的节点也要一起删除，包容网关的聚合结束节点也要一起删除
- [删除限制]默认分支的所有连线和节点都不允许单独删除
- [默认分支新增限制]包容网关默认分支不允许添加新的节点，包容网关的分流节点 和 聚合结束节点之间不允许添加新的节点

9、每组包容网关之间的节点需要添加完整的 forkGatewayId 和 joinGatewayId 属性，请添加
// 所属分流网关 ID
forkGatewayId?: string;
// 所属聚合网关 ID
joinGatewayId?: string;

- 建议在连线时及时添加这些属性，避免出现错误
- 嵌套包容网关需要添加自己的 forkGatewayId 和 joinGatewayId 属性，请勿混淆

使用普通节点，{type : "rect"}，拖入画布，扩充包容网关分支节点时，节点的 forkGatewayId 和 joinGatewayId 属性没有添加上，请帮我设计一下可以在哪一个环节添加这两个配置属性

当一个包容网关作为另外一个包容网关的一个分支时，包容网关的分流节点和聚合结束节点 需要扩充 forkGatewayId 和 joinGatewayId 属性

10、帮我在 H:\2026code\demo\doc-flow\rsbuild-logicflow-project\src\views\flow\design\common\lf-element 目录下再继续实现 自定义 开始和结束节点
- 开始节点是一个圆
- 结束节点是一个圆，中间是一个实心矩形
- 节点需要支持大小和颜色的配置，具体功能和实现可以参考 lf-element 下的排他网关和并行网关自定义节点的实现
- 自定义的节点对外参数结构需要和排他网关和并行网关的参数结构一致

**排他网关实现**
H:\2026code\demo\doc-flow\rsbuild-logicflow-project\src\views\flow\design\common\lf-element\exclusive-gateway-model.ts
H:\2026code\demo\doc-flow\rsbuild-logicflow-project\src\views\flow\design\common\lf-element\exclusive-gateway-node.tsx
**包容网关自定义节点实现**
H:\2026code\demo\doc-flow\rsbuild-logicflow-project\src\views\flow\design\common\lf-element\inclusive-gateway-model.ts
H:\2026code\demo\doc-flow\rsbuild-logicflow-project\src\views\flow\design\common\lf-element\inclusive-gateway-node.tsx

- 开始节点和结束节点禁止编辑修改名称，禁止右键菜单触发
- 开始节点和结束节点文字偏移的计算不正确，默认不设置时应该在圆心居中显示

11、节点配置
- 节点正常点击时需要在页面的右侧弹出一个抽屉进行一些属性配置
- 节点名称修改
- 节点关联审批用户
- 抽屉底部的保存和取消按钮
- 排他网关和并行网关节点点击时需要禁用抽屉出现，
- 开始节点和结束节点也不需要进行配置，点击时无需出现抽屉，请修改

12、文本节点编辑
- 节点名称双击时，节点文本或者边文本快速进入编辑状态
- 双击时和节点打开抽屉的单击事件禁止冲突，请修改
- 开始节点和结束节点双击时禁止进入编辑状态，请修改

13、项目规范
帮我整理一份AI开发过程中，目录、文件名称、变量名称 的要求规范
- 目录使用中划线、文件名称使用中划线、
- 变量名称使用小驼峰
- 函数名称使用小驼峰
- 类名称使用大驼峰
- 函数参数使用小驼峰
- 函数返回值使用小驼峰
- 函数参数名称使用小驼峰
- 类成员变量名称使用小驼峰

项目主要是使用vue3 + typescript + vue-router + element-plus + axios + pinia
- vue3 组件开发全部使用tsx语法编写
- vue3 规范必须使用vue官方推荐的组件规范，请检查修改



经过测试，开始节点和结束节点的右键操作还是可以触发出现，请检查继续修改

现在开始和结束节点的名称文字默认没有居中，请继续检查修改


------------------------------------------------------------------


------------------------------------------------------------------


------------------------------------------------------------------


------------------------------------------------------------------


------------------------------------------------------------------


------------------------------------------------------------------


------------------------------------------------------------------


------------------------------------------------------------------


------------------------------------------------------------------


------------------------------------------------------------------


------------------------------------------------------------------


------------------------------------------------------------------
